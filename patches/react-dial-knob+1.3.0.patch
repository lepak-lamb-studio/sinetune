diff --git a/node_modules/react-dial-knob/dist/react-dial-knob.es.js b/node_modules/react-dial-knob/dist/react-dial-knob.es.js
index 74982cf..e081b06 100644
--- a/node_modules/react-dial-knob/dist/react-dial-knob.es.js
+++ b/node_modules/react-dial-knob/dist/react-dial-knob.es.js
@@ -33,7 +33,6 @@ var KnobArea = /** @class */ (function () {
             _this.addWindowEventListeners('mouse');
             var pageX = event.pageX, pageY = event.pageY, clientX = event.clientX, clientY = event.clientY;
             _this.updateAreaLocation({ pageX: pageX, pageY: pageY, clientX: clientX, clientY: clientY });
-            _this.updateAngleValue(pageX, pageY);
         };
         this.handleOnMouseMove = function (event) {
             if (!_this.isInteracting) {
@@ -85,6 +84,7 @@ var KnobArea = /** @class */ (function () {
         };
         this.onAngleChange = props.onAngleChange;
         this.onValueChange = props.onValueChange;
+        this.displayValue = props.displayValue;
         this.onInteractionChange = props.onInteractionChange;
         this._locationX = 0;
         this._locationY = 0;
@@ -109,9 +109,11 @@ var KnobArea = /** @class */ (function () {
         this.min = props.min;
         this.max = props.max;
         this.step = props.step;
+        this.offsetAngle = props.offsetAngle || 0;
         this.diameter = props.diameter;
         this.onAngleChange = props.onAngleChange || this.onAngleChange;
         this.onValueChange = props.onValueChange || this.onValueChange;
+        this.displayValue = props.displayValue || this.displayValue;
         this.onInteractionChange =
             props.onInteractionChange || this.onInteractionChange;
         this.spaceMaxFromZero =
@@ -161,7 +163,7 @@ var KnobArea = /** @class */ (function () {
                 return;
             }
             this._value = newValue;
-            if (this.onValueChange) {
+            if (this._isInteracting && this.onValueChange) {
                 this.onValueChange(this._value);
             }
         },
@@ -250,34 +252,10 @@ var KnobArea = /** @class */ (function () {
         };
     };
     KnobArea.prototype.updateAreaLocation = function (eventCoords) {
-        var areaRadius = this.diameter / 2;
-        // See article with explanation at: https://www.kirupa.com/html5/get_element_position_using_javascript.htm
-        var x = 0;
-        var y = 0;
-        var el = this.refElement.current;
-        var transformXY = this.getComputedTransformXY(el);
-        while (el) {
-            if (el.tagName.toUpperCase() === 'BODY') {
-                // deal with browser quirks with body/window/document and page scroll
-                var xScroll = el.scrollLeft || document.documentElement.scrollLeft;
-                var yScroll = el.scrollTop || document.documentElement.scrollTop;
-                x += el.offsetLeft - xScroll + el.clientLeft;
-                y += el.offsetTop - yScroll + el.clientTop;
-            }
-            else {
-                // for all other non-BODY elements
-                x += el.offsetLeft - el.scrollLeft + el.clientLeft;
-                y += el.offsetTop - el.scrollTop + el.clientTop;
-            }
-            x += transformXY.x;
-            y += transformXY.y;
-            el = el.offsetParent;
-            transformXY = this.getComputedTransformXY(el);
-        }
-        this._locationX = x + areaRadius;
-        this._locationY = y + areaRadius;
-        this._locationX += eventCoords.pageX - eventCoords.clientX;
-        this._locationY += eventCoords.pageY - eventCoords.clientY;
+        if (!this.refElement.current) return;
+        const rect = this.refElement.current.getBoundingClientRect();
+        this._locationX = rect.left + rect.width / 2 + window.scrollX + eventCoords.pageX - eventCoords.clientX;
+        this._locationY = rect.top + rect.height / 2 + window.scrollY + eventCoords.pageY - eventCoords.clientY;
     };
     KnobArea.prototype.calcDegreeOfRotation = function (pageX, pageY) {
         var rad = Math.atan2(pageX - this._locationX, pageY - this._locationY);
@@ -286,7 +264,7 @@ var KnobArea = /** @class */ (function () {
     };
     KnobArea.prototype.valueFromAngle = function (angle) {
         var _this = this;
-        var angleAsPercent = angle / (this.numSteps * this.valsDistribution);
+        var angleAsPercent = ((((angle - this.offsetAngle) % 360) + 360) % 360) / (this.numSteps * this.valsDistribution);
         var totalValue = this.numSteps * this.step;
         var val = this.min + angleAsPercent * totalValue;
         if (val > this.max + this.step / 2) {
@@ -301,7 +279,7 @@ var KnobArea = /** @class */ (function () {
         return closest;
     };
     KnobArea.prototype.angleFromValue = function (value) {
-        return Math.ceil(((value - this.min) / this.step) * this.valsDistribution);
+        return ((((Math.ceil(((value - this.min) / this.step) * this.valsDistribution) + this.offsetAngle) % 360) + 360) % 360);
     };
     KnobArea.prototype.updateAngleValue = function (pageX, pageY) {
         var _this = this;
@@ -477,7 +455,7 @@ function HighContrast(props) {
     var angleChangeHandler = composeTwo(setAngle, props.onAngleChange);
     var interactionChangeHandler = composeTwo(setIsActive, props.onInteractionChange);
     return (React.createElement(SkinWrap, { style: props.style },
-        React.createElement(Knob, { diameter: props.diameter, value: props.value, min: props.min, max: props.max, step: props.step, jumpLimit: props.jumpLimit, spaceMaxFromZero: props.spaceMaxFromZero, ariaLabelledBy: props.ariaLabelledBy, ariaValueText: props.ariaValueText, knobStyle: __assign({ cursor: 'pointer' }, props.knobStyle), onAngleChange: angleChangeHandler, onInteractionChange: interactionChangeHandler, onValueChange: props.onValueChange },
+        React.createElement(Knob, { diameter: props.diameter, value: props.value, min: props.min, max: props.max, step: props.step, offsetAngle: props.offsetAngle, jumpLimit: props.jumpLimit, spaceMaxFromZero: props.spaceMaxFromZero, ariaLabelledBy: props.ariaLabelledBy, ariaValueText: props.ariaValueText, knobStyle: __assign({ cursor: 'pointer' }, props.knobStyle), onAngleChange: angleChangeHandler, onInteractionChange: interactionChangeHandler, onValueChange: props.onValueChange, displayValue: props.displayValue },
             React.createElement(React.Fragment, null,
                 React.createElement("svg", { viewBox: "0 0 100 100", transform: "rotate(" + angle + ")", style: { transform: "rotate(" + angle + "deg)" } },
                     React.createElement("path", { fill: bgrColor, d: "M50 0A50 50 0 000 50a50 50 0 0050 50 50 50 0 0050-50A50 50 0 0050 0zm0 2a48 48 0 0148 48 48 48 0 01-48 48A48 48 0 012 50 48 48 0 0150 2z" }),
@@ -491,7 +469,7 @@ function HighContrast(props) {
                         top: 'calc(50% - 0.6em)',
                         userSelect: 'none',
                         color: '#fff',
-                    } }, props.value))),
+                    } }, props.displayValue ? props.displayValue(props.value, isActive) : props.value))),
         props.children));
 }
 
diff --git a/node_modules/react-dial-knob/dist/src/Knob.d.ts b/node_modules/react-dial-knob/dist/src/Knob.d.ts
index dd06f83..0fd8d75 100644
--- a/node_modules/react-dial-knob/dist/src/Knob.d.ts
+++ b/node_modules/react-dial-knob/dist/src/Knob.d.ts
@@ -6,6 +6,7 @@ export interface KnobProps {
     min: number;
     max: number;
     step: number;
+    offsetAngle?: number;
     jumpLimit?: number;
     spaceMaxFromZero?: boolean;
     ariaLabelledBy?: string;
@@ -15,6 +16,7 @@ export interface KnobProps {
     onAngleChange?: (angle: number) => void;
     onInteractionChange?: (isInteracting: boolean) => void;
     onValueChange?: (value: number) => void;
+    displayValue?: (value: number, isInteracting: boolean) => string;
 }
 export declare function useKnobAreaClass(props: KnobProps): [React.RefObject<HTMLDivElement>, KnobArea | null, Error | null];
 export default function Knob(props: KnobProps): JSX.Element;
